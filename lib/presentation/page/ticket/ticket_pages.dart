import 'package:flutter/material.dart';

class TicketPages extends StatelessWidget {
  const TicketPages({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Ticket"));
    ;
  }
}
